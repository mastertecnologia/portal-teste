# Referências visuais ERP premium

| Arquivo | Descrição |
|---------|-----------|
| `pgm_orcamentos_premium.html` | Extrato do mockup completo — módulo Orçamentos (`pg-lista` … `pg-sucesso`) |
| `ordens_servico_premium.html` | Referência alternativa de OS (tema escuro; legado) |
| `pgm_erp_completo.html` | Mockup SPA completo (~100 telas). **Coloque o arquivo aqui** se ainda não estiver no repositório (ex.: cópia de `Downloads/pgm_erp_completo.html`). |

Implementação Cake: layout `erp_prototype`, CSS `webroot/dist/css/pgm-erp-prototype.css`, rotas `*-prototype/*`.
