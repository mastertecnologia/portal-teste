{
  "name": "portal-teste",
  "private": true,
  "description": "Raiz do repo PHP; scripts Node.js do dashboard em dashboard-react/",
  "scripts": {
    "test": "npm run test --prefix=dashboard-react"
  }
}
