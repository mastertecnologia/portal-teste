{
  "name": "portal-teste",
  "private": true,
  "description": "Raiz do repo PHP; scripts Node.js do dashboard em dashboard-react/",
  "scripts": {
    "test": "npm run test --prefix=dashboard-react",
    "build": "npm run build --prefix=dashboard-react",
    "build:tickets": "npm run build --prefix=dashboard-react",
    "capture:admin-audit-round3": "node scripts/capture-admin-audit-round3.mjs"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
